<Window x:Class="FileVViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileVViewer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Loaded="Window_Loaded">
    <Grid  x:Name="MGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <ListView x:Name="DirFileList" SelectionChanged="DirFileList_SelectionChanged" MouseDoubleClick="DirFileList_MouseDoubleClick" KeyUp="DirFileList_KeyUp">
            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>
        <GridSplitter Grid.Column ="0" Background="LightSlateGray" Width="3"
                HorizontalAlignment="Right" VerticalAlignment="Stretch"/>

        <ScrollViewer Grid.Column="1">
            <TextBlock x:Name="RightTextViewer"  TextWrapping="Wrap" Margin="10,0,0,0"/>
        </ScrollViewer>
    </Grid>
</Window>
